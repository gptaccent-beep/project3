type Contact={email:string;phone:string;address:Record<string,string>;hours:Record<string,string>}
export default function ContactSection({contact,locale,labels}:{contact?:Contact;locale:string;labels:Record<string,string>}){if(!contact)return null;const address=contact.address?.[locale]||contact.address?.ar||"",hours=contact.hours?.[locale]||contact.hours?.ar||"";return <section className="section contact-section" id="contact" aria-labelledby="contact-title"><div className="wrap"><div className="contact-card reveal"><div className="contact-intro"><p className="eyebrow">{labels.contactEyebrow||"Contact"}</p><h2 id="contact-title">{labels.contactTitle||"Let’s talk"}</h2><p>{labels.contactBody||"Questions about an order or our honey? Contact us directly."}</p></div><div className="contact-grid"><a href={`mailto:${contact.email}`}><span>{labels.contactEmail||"Email"}</span><b>{contact.email}</b></a><a href={`tel:${contact.phone.replace(/[^+\d]/g,"")}`}><span>{labels.contactPhone||"Phone"}</span><b dir="ltr">{contact.phone}</b></a><div><span>{labels.contactAddress||"Address"}</span><b>{address}</b></div><div><span>{labels.contactHours||"Hours"}</span><b>{hours}</b></div></div></div></div></section>}
