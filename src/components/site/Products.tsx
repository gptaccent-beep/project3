"use client"
import {FormEvent,useMemo,useState} from "react"
export type Product={id:string;name:string;badge?:string;image:string;alt:string;notes:string[];price:number;unit:string;description:string;stock:string;categoryId:string;category:string}
type Labels=Record<string,string>
export default function Products({eyebrow,title,body,products,categories,locale,whatsappNumber,labels,bundles=[],promotions=[]}:{eyebrow:string;title:string;body:string;products:Product[];categories:{id:string;name:string}[];locale:string;whatsappNumber:string;labels:Labels;bundles?:any[];promotions?:any[]}){const [filter,setFilter]=useState("all"),[expanded,setExpanded]=useState(false),[selected,setSelected]=useState<Product|null>(null),[qty,setQty]=useState(1),[phone,setPhone]=useState(""),[location,setLocation]=useState("");const filtered=useMemo(()=>filter==="all"?products:products.filter(p=>p.categoryId===filter),[filter,products]),shown=expanded?filtered:filtered.slice(0,6);function chooseFilter(id:string){setFilter(id);setExpanded(false)}function submit(e:FormEvent){e.preventDefault();if(!selected)return;const messages:any={ar:`مرحبًا بوزيد، أريد طلب:\nالمنتج: ${selected.name}\nالكمية: ${qty}\nهاتف الزبون: ${phone}\nمكان التوصيل: ${location}`,en:`Hello BOUZID, I would like to order:\nProduct: ${selected.name}\nQuantity: ${qty}\nCustomer phone: ${phone}\nDelivery location: ${location}`,fr:`Bonjour BOUZID, je souhaite commander :\nProduit : ${selected.name}\nQuantité : ${qty}\nTéléphone : ${phone}\nLieu de livraison : ${location}`};window.open(`https://wa.me/${whatsappNumber.replace(/\D/g,"")}?text=${encodeURIComponent(messages[locale]||messages.ar)}`,"_blank","noopener,noreferrer")}
return <section className="section section-dark comb-bg commerce" id="products" aria-labelledby="products-title"><div className="wrap"><div className="section-head"><p className="eyebrow reveal">{eyebrow}</p><h2 id="products-title" className="reveal" data-delay="90">{title}</h2><p className="lede reveal" data-delay="160">{body}</p></div><div className="category-bar" role="tablist" aria-label={labels.categoryLabel}><button className={filter==="all"?"active":""} onClick={()=>chooseFilter("all")}>{labels.filterAll}</button>{categories.map(c=><button key={c.id} className={filter===c.id?"active":""} onClick={()=>chooseFilter(c.id)}>{c.name}</button>)}</div><div className="product-grid commercial-grid">{shown.map((p,i)=><article key={p.id} className="card-3d glass lift reveal is-in" data-delay={Math.min(60+i*60,360)}><div className="card-media">{p.badge&&<span className="badge">{p.badge}</span>}<img src={p.image} alt={p.alt} loading="lazy" width={1200} height={800}/></div><div className="card-body"><div className="product-meta"><span>{p.category}</span><span className={`stock-dot ${p.stock}`}>{labels[`stock_${p.stock}`]}</span></div><h3>{p.name}</h3><p className="product-description">{p.description}</p><ul className="card-notes">{p.notes.map(n=><li key={n} className="note">{n}</li>)}</ul><div className="card-foot commercial-foot"><p className="price">{locale==="ar"?`${p.price} د.م.`:`${p.price} MAD`}<small>{p.unit}</small></p><button className="whatsapp-buy" disabled={p.stock==="out_of_stock"} onClick={()=>{setSelected(p);setQty(1)}}><span>◉</span>{labels.orderWhatsApp}</button></div><p className="delivery-trust">✓ {labels.deliveryTrust}</p></div></article>)}</div>{filtered.length>6&&<button className="expand-button products-expand" type="button" aria-expanded={expanded} onClick={()=>setExpanded(v=>!v)}>{expanded?(labels.showLess||"Show less"):(labels.showAllProducts||"Show all products")}<span>{expanded?"↑":"↓"}</span></button>} {(bundles.length>0||promotions.length>0)&&<div className="all-offers"><p className="eyebrow">{labels.featuredOffers}</p><div className="offers-grid">{bundles.map((b:any,i:number)=><div className="offer-card" key={`${b.name}-${i}`}><h3>{b.name}</h3><span>{labels.bundleLabel} · {locale==="ar"?`${b.price} د.م.`:`${b.price} MAD`}</span></div>)}{promotions.map((p:any,i:number)=><div className="promo-code" key={`${p.code}-${i}`}><span>{labels.promoLabel}</span><b>{p.code}</b><em>-{p.value}%</em></div>)}</div></div>}</div>{selected&&<div className="order-modal" role="dialog" aria-modal="true" aria-labelledby="order-title" onMouseDown={e=>e.target===e.currentTarget&&setSelected(null)}><form onSubmit={submit}><button className="modal-close" type="button" onClick={()=>setSelected(null)} aria-label={labels.cancel}>×</button><p className="admin-kicker">WHATSAPP ORDER</p><h2 id="order-title">{labels.orderTitle}</h2><p>{labels.orderIntro}</p><div className="order-product"><img src={selected.image} alt=""/><div><b>{selected.name}</b><span>{locale==="ar"?`${selected.price} د.م.`:`${selected.price} MAD`}</span></div></div><label>{labels.quantity}<input type="number" min="1" max="99" value={qty} onChange={e=>setQty(+e.target.value)} required/></label><label>{labels.buyerPhone}<input type="tel" value={phone} placeholder={labels.phonePlaceholder} onChange={e=>setPhone(e.target.value)} required/></label><label>{labels.location}<input value={location} placeholder={labels.locationPlaceholder} onChange={e=>setLocation(e.target.value)} required/></label><div className="modal-actions"><button type="button" className="modal-cancel" onClick={()=>setSelected(null)}>{labels.cancel}</button><button className="whatsapp-submit">{labels.sendOrder}<span>↗</span></button></div></form></div>}</section>}
