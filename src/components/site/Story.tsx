"use client"
import {useState} from "react"
import Bees from "./Bees"
export type Step={num:string;title:string;body:string}
const DEFAULT_STEPS:Step[]=[{num:"Step 01",title:"The bloom",body:"In April the meadows open and our hives follow the mountain flowers."},{num:"Step 02",title:"The forage",body:"Each worker visits hundreds of flowers every day."},{num:"Step 03",title:"The ripening",body:"Nectar ripens naturally in the comb."},{num:"Step 04",title:"The harvest",body:"Frames are lifted by hand and cold-extracted."},{num:"Step 05",title:"The jar",body:"Honey is poured into glass and batch tested."}]
export default function Story({eyebrow="Our story",title="The journey from flower to honey",body="Five steps, one season, zero shortcuts.",image="/assets/bees-pollination.webp",steps=DEFAULT_STEPS,readMore="Read more",showLess="Show less"}:{eyebrow?:string;title?:string;body?:string;image?:string;steps?:Step[];readMore?:string;showLess?:string}){const [expanded,setExpanded]=useState(false),visible=expanded?steps:steps.slice(0,3);return <section className="section section-light" id="story" aria-labelledby="story-title"><div className="wrap story"><div className="story-visual img-reveal reveal-scale"><img src={image} alt="" loading="lazy" width={1200} height={1600}/><Bees variant="compact"/></div><div><div className="section-head"><p className="eyebrow reveal">{eyebrow}</p><h2 id="story-title" className="reveal" data-delay="90">{title}</h2><p className="lede reveal" data-delay="150">{body}</p></div><div className="timeline"><i className="timeline-progress" aria-hidden="true"/>{visible.map(s=><article key={s.title} className="step is-active" tabIndex={0}><p className="step-num">{s.num}</p><h3>{s.title}</h3><p>{s.body}</p></article>)}</div>{steps.length>3&&<button className="expand-button" type="button" aria-expanded={expanded} onClick={()=>setExpanded(v=>!v)}>{expanded?showLess:readMore}<span>{expanded?"↑":"↓"}</span></button>}</div></div></section>}
